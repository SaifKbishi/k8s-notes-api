# Service
apiVersion: v1
kind: Service
metadata:
  name: k8s-notes-api-service
spec:
  type: NodePort
  selector:
    app: k8s-notes-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080